{% extends 'base.html.twig' %}

{% block title %}Définir le chemin du dossier de sauvegarde{% endblock %}

{% block body %}
    <h1>Définir le chemin du dossier de sauvegarde</h1>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}
    {% for message in app.flashes('error') %}
        <div class="alert alert-danger">{{ message }}</div>
    {% endfor %}

    {{ form_start(form) }}
        {{ form_row(form.path) }}
        {{ form_row(form.save) }}
    {{ form_end(form) }}
{% endblock %}
